name: SkyRandomizer
version: '${version}'
main: de.joker.randomizer.SkyRandomizer
api-version: '1.21'
load: STARTUP
authors:
  - 'InvalidJoker'

dependencies:
  server:
    ProtocolLib:
      required: true
      load: BEFORE